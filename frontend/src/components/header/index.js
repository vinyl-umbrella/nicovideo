import style from './style.css';

const Header = () => (
  <header class={style.header}>
    <a href='/' class={style.title}>
      <h1>ニコ動 (Re:仮) 検索サービス (仮)</h1>
    </a>
  </header>
);

export default Header;
