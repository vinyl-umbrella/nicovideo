{
  "private": true,
  "name": "nicosearch",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "dev": "preact watch",
    "fmt": "prettier --write --ignore-path .gitignore './src/**/*.{html,css,js,jsx,json}'"
  },
  "devDependencies": {
    "preact-cli": "^3.5.1",
    "prettier": "^3.3.2"
  },
  "dependencies": {
    "preact": "^10.11.3",
    "preact-render-to-string": "^5.2.6"
  }
}
